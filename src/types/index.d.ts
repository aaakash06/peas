export interface TeacherDBType {
  id: number;
  name: string;
  subject: string;
  topic: string;
  initials: string;
  HPC: number;
}
export interface TeacherType {
  name: string;
  subject: string;
  topic: string;
  initials: string;
  HPC: number;
}
